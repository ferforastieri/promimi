FROM node:22-alpine AS build
RUN corepack enable
WORKDIR /app
COPY . .
RUN pnpm install --frozen-lockfile=false && pnpm --filter @promimi/infrastructure build && pnpm --filter @promimi/api build
CMD ["pnpm", "--filter", "@promimi/api", "exec", "tsx", "src/server.ts"]
