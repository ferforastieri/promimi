import { Link, Form } from "react-router";
import type { Offer } from "../catalog/types";
import { BrandMark } from "@promimi/design-system";
import { brl } from "../catalog/server";

export function Header() { return <><div className="topline"><span>Curadoria de ofertas verificadas</span><span>Horários em Brasília (BRT)</span></div><header><Link to="/" className="logo" aria-label="Promimi, início"><BrandMark className="h-7 w-7" label=""/><span>pro</span><b>mimi</b><i>•</i></Link><Form action="/buscar" className="search"><input name="q" placeholder="O que você quer encontrar?"/><button aria-label="Buscar">⌕</button></Form><nav><Link to="/buscar">Explorar</Link><Link to="/conta">Minha conta</Link><Link to="/entrar" className="nav-join">Entrar</Link></nav></header></> }
export function Footer() { return <footer><div className="footer-logo"><span className="logo"><BrandMark className="h-7 w-7" label=""/><span>pro</span><b>mimi</b><i>•</i></span><p>Oferta boa é aquela que ainda existe quando você chega.</p></div><div><strong>Promimi</strong><Link to="/buscar">Ofertas</Link><Link to="/entrar">Minha conta</Link></div><div><strong>Transparência</strong><Link to="/privacidade">Privacidade</Link><Link to="/termos">Termos de uso</Link><Link to="/cookies">Cookies</Link></div><p className="affiliate">Alguns links podem render comissão, sem custo adicional para você.</p></footer> }
export function OfferCard({ offer, featured = false }: { offer: Offer; featured?: boolean }) { return <article className={`offer-card ${featured ? "offer-card--featured" : ""}`}><div className={`product-visual visual-${offer.category?.slug ?? "geral"}`}><span>{offer.category?.name ?? "Oferta"}</span><div className="product-orb" aria-hidden="true"/></div><div className="offer-body"><div className="offer-meta"><span>{offer.store.name}</span>{offer.discountPercent && <b>-{offer.discountPercent}%</b>}</div><h3><Link to={`/oferta/${offer.slug}`}>{offer.title}</Link></h3><div className="prices">{offer.originalPrice && <del>{brl(offer.originalPrice)}</del>}<strong>{brl(offer.currentPrice)}</strong></div>{offer.couponCode ? <div className="coupon"><small>CUPOM</small><code>{offer.couponCode}</code></div> : <span className="verified">✓ preço verificado agora</span>}<Link className="card-link" to={`/oferta/${offer.slug}`}>Ver oferta <span>→</span></Link></div></article> }
