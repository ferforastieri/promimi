import { useState } from "react";
import { Link } from "react-router";
import { Button, Card, NotFoundPage, PageLayout, StatusPill, useToast } from "@promimi/design-system";
import { Footer, Header, OfferCard } from "../../shell/components";
import { apiOffers, brl } from "../server";
import { OfferActions } from "../../offer/OfferActions";

const visualTones: Record<string, string> = { casa: "bg-[#fff4f0]", esporte: "bg-pine-soft", tecnologia: "bg-info-soft", audio: "bg-[#f4efff]" };
export async function loader({ params }: { params: { slug: string } }) { const offers = await apiOffers(); const offer = offers.find((item) => item.slug === params.slug); return { offer, related: offers.filter((item) => item.id !== offer?.id).slice(0, 3) }; }
export function meta({ data }: { data: Awaited<ReturnType<typeof loader>> }) { const offer = data?.offer; if (!offer) return [{ title: "Oferta — Promimi" }]; const origin = process.env.APP_URL ?? "http://localhost:3000"; return [{ title: `${offer.title} — Promimi` }, { name: "description", content: `${offer.title} por ${brl(offer.currentPrice)} na ${offer.store.name}.` }, { property: "og:title", content: offer.title }, { property: "og:description", content: `Oferta verificada na ${offer.store.name}.` }, { property: "og:type", content: "product" }, { property: "og:url", content: `${origin}/oferta/${offer.slug}` }]; }

export default function Offer({ loaderData }: { loaderData: Awaited<ReturnType<typeof loader>> }) {
  const { offer, related } = loaderData; const { showToast } = useToast();
  if (!offer) return <NotFoundPage title="Oferta não encontrada" description="Esta oferta não está mais publicada ou o endereço é inválido." />;
  const trackedUrl = `${process.env.API_URL ?? "http://localhost:3001"}/api/v1/offers/${offer.id}/go?source=site`; const tone = visualTones[offer.category?.slug ?? ""] ?? "bg-info-soft";
  const [copying, setCopying] = useState(false); const [sharing, setSharing] = useState(false);
  const copyCoupon = async () => { if (!navigator.clipboard) return; setCopying(true); try { await navigator.clipboard.writeText(offer.couponCode ?? ""); showToast({ title: "Cupom copiado", tone: "success" }); } finally { setCopying(false); } };
  const share = async () => { if (!navigator.share) { showToast({ title: "Compartilhamento indisponível", description: "Copie o endereço desta página para compartilhar.", tone: "info" }); return; } setSharing(true); try { await navigator.share({ title: offer.title, url: window.location.href }); } finally { setSharing(false); } };
  return <><Header /><PageLayout><Link className="mb-5 inline-flex text-sm font-semibold text-ink/60 transition hover:text-brand" to="/buscar">← Voltar às ofertas</Link><section className="grid gap-5 lg:grid-cols-[1fr_.95fr]"><Card className={`relative min-h-[330px] overflow-hidden p-5 sm:min-h-[430px] ${tone}`}><StatusPill tone="neutral">{offer.category?.name ?? "Oferta"}</StatusPill><div className="absolute bottom-[15%] right-[18%] h-[190px] w-[190px] rotate-[-13deg] rounded-[40px] border-[22px] border-white/70 shadow-[inset_0_0_0_20px_rgba(42,49,65,.07)] sm:h-[260px] sm:w-[260px] sm:border-[28px]" /></Card><Card className="flex flex-col justify-center p-6 sm:p-8"><div className="flex flex-wrap items-center gap-2"><StatusPill tone="green">Verificada agora</StatusPill><span className="text-xs font-medium text-ink/50">{offer.store.name}</span></div><h1 className="mt-5 text-[clamp(30px,4vw,44px)] font-semibold leading-[1.08] tracking-[-.055em]">{offer.title}</h1><div className="mt-6 flex flex-wrap items-baseline gap-3">{offer.originalPrice && <del className="text-sm text-ink/42">de {brl(offer.originalPrice)}</del>}<strong className="text-[42px] font-semibold tracking-[-.065em]">{brl(offer.currentPrice)}</strong>{offer.discountPercent && <StatusPill tone="orange">economize {offer.discountPercent}%</StatusPill>}</div>{offer.couponCode && <div className="mt-5 flex items-center gap-2 rounded-xl border border-dashed border-brand/40 bg-brand-soft px-3 py-2.5 text-brand"><span className="text-[10px] font-semibold uppercase tracking-wide">Cupom</span><code className="text-xs font-bold">{offer.couponCode}</code><Button className="ml-auto" variant="ghost" size="sm" disabled={copying} onClick={() => void copyCoupon()}>{copying ? "Copiando…" : "Copiar"}</Button></div>}<a className="mt-6 inline-flex min-h-12 items-center justify-between rounded-xl bg-brand px-5 text-sm font-semibold text-white shadow-[0_7px_18px_rgba(238,77,45,.22)] transition hover:bg-brand-dark" href={trackedUrl} target="_blank" rel="noreferrer">Ir para a loja <span>↗</span></a><p className="mt-4 text-xs leading-6 text-ink/52">Você será direcionado para {offer.store.name}. Podemos receber comissão por esta compra.</p><Button className="mt-4 self-start" variant="ghost" size="sm" disabled={sharing} onClick={() => void share()}>{sharing ? "Compartilhando…" : "↗ Compartilhar"}</Button></Card></section><section className="grid gap-5 py-10 lg:grid-cols-[minmax(0,760px)_1fr]"><Card className="p-6"><p className="text-[11px] font-semibold uppercase tracking-[.12em] text-ink/42">Sobre esta oferta</p><h2 className="mt-2 text-2xl font-semibold tracking-[-.04em]">Vale a pena?</h2><p className="mt-5 text-sm leading-7 text-ink/65">{offer.description ?? "Preço encontrado pela equipe Promimi e conferido antes da publicação. Estoque, frete e condições podem variar conforme o seu CEP."}</p><p className="mt-3 text-sm leading-7 text-ink/65">Última verificação em horário de Brasília. Caso o preço tenha mudado, avise nossa equipe.</p></Card></section><OfferActions offerId={offer.id} /><section className="border-t border-line pt-9"><div className="mb-5"><p className="text-[11px] font-semibold uppercase tracking-[.12em] text-ink/42">Continue garimpando</p><h2 className="mt-1 text-2xl font-semibold tracking-[-.04em]">Você também pode gostar</h2></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{related.map((item) => <OfferCard key={item.id} offer={item} />)}</div></section></PageLayout><Footer /></>;
}
