import { useState } from "react";
import { Link } from "react-router";
import { Header } from "../../shell/components";
import { apiRequest } from "../../../api/client";
export default function Recover() { const [notice, setNotice] = useState(""); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const email = String(new FormData(event.currentTarget).get("email")); try { await apiRequest("/auth/request-password-reset", { method: "POST", body: JSON.stringify({ email }) }); setNotice("Se este e-mail estiver cadastrado, enviamos as instruções de recuperação."); } catch { setNotice("Não foi possível solicitar a recuperação agora. Tente novamente."); } }; return <><Header /><main className="grid min-h-[calc(100vh-88px)] items-center gap-10 bg-[linear-gradient(105deg,#d9efe5_0_55%,#fffdf8_55%)] px-4 py-14 sm:px-6 md:grid-cols-[1fr_420px] md:px-8 lg:px-12 xl:px-20"><div><p className="mb-3 font-mono text-[10px] tracking-[.1em] text-ink/70">RECUPERAR ACESSO</p><h1 className="font-display text-[clamp(48px,5vw,58px)] font-bold leading-[.98] tracking-[-.065em]">Vamos achar<br /><em className="text-brand">seu radar.</em></h1><p className="mt-6 max-w-[380px] leading-relaxed text-ink/70">Informe seu e-mail. Por segurança, mostramos a mesma confirmação para qualquer endereço.</p></div><form className="rounded-xl border border-line bg-white p-8 shadow-[12px_12px_0_#cde4da]" onSubmit={submit}><h2 className="font-display text-[34px] font-bold leading-[.95] tracking-[-.05em]">Redefinir senha</h2><label className="mt-5 grid gap-1.5 text-xs font-bold">E-mail<input className="rounded-md border border-[#d7d2c9] p-3" required name="email" type="email" placeholder="voce@exemplo.com" /></label><button className="my-5 w-full cursor-pointer rounded-md bg-brand p-3 font-extrabold text-white">Enviar instruções</button>{notice && <p className="rounded-md bg-pine-soft p-2.5 text-xs leading-relaxed text-pine">{notice}</p>}<hr className="mt-6 border-0 border-t border-line" /><p className="mt-4 text-xs"><Link className="font-bold text-brand" to="/entrar">Voltar para entrar</Link></p></form></main></>; }
