import { useState } from "react";
import { Link, useSearchParams } from "react-router";
import { Header } from "../components";
const apiOrigin = import.meta.env.VITE_API_URL ?? (import.meta.env.DEV || typeof window === "undefined" ? "http://localhost:3001" : window.location.origin);
const api = `${apiOrigin}/api/v1`;
export default function ResetPassword() { const [params] = useSearchParams(); const [notice, setNotice] = useState(""); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const password = String(form.get("password")); if (password !== String(form.get("confirmation"))) { setNotice("As senhas precisam ser iguais."); return; } try { const response = await fetch(`${api}/auth/reset-password`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ token: params.get("token"), password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); setNotice("Senha redefinida. Você já pode entrar."); } catch (error) { setNotice(error instanceof Error ? error.message : "Não foi possível redefinir a senha."); } }; return <><Header/><main className="auth-page"><div><p className="eyebrow">NOVA SENHA</p><h1>Volte a<br/><em>garimpar.</em></h1><p>Escolha uma senha com no mínimo dez caracteres. O link expira após uma hora.</p></div><form className="auth-card" onSubmit={submit}><h2>Definir nova senha</h2><label>Nova senha<input required name="password" type="password" minLength={10}/></label><label>Confirme a senha<input required name="confirmation" type="password" minLength={10}/></label><button>Salvar nova senha</button>{notice && <p className="auth-notice">{notice}</p>}<hr/><p><Link to="/entrar">Voltar para entrar</Link></p></form></main></>; }
