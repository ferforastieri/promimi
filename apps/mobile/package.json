{
  "name": "@promimi/mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "android:debug": "pnpm sync && cd android && ./gradlew assembleDebug",
    "android:apk": "node scripts/require-site-url.mjs && pnpm sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
